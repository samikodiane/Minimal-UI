export {
  ACCENT_OPACITY,
  accentFromSecondary,
  clampBorderRadius,
  clampBorderWidth,
  colorWithOpacity,
  ColorsProvider,
  DEFAULT_BORDER_RADIUS,
  DEFAULT_BORDER_WIDTH,
  DEFAULT_PRIMARY,
  DEFAULT_SECONDARY,
  MAX_BORDER_RADIUS,
  MAX_BORDER_WIDTH,
  MIN_BORDER_RADIUS,
  MIN_BORDER_WIDTH,
  useColors,
} from './colors';
export type {
  ColorsContextValue,
  ColorsProviderProps,
  ThemeBorders,
  ThemeColors,
} from './colors';
